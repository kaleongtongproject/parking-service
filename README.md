# Parking Lot Starter (Spring Boot)

## What

A tiny Spring Boot service demonstrating concurrency with a `Semaphore`.

## Run

1. Build & run with Maven:

```bash
mvn clean package
mvn spring-boot:run
```
